-
  let title = "404";
  let breadcrumbs = [
    {
      href: './',
      label: 'Главная'
    },
    {
      href: null,
      label: '404',
      last: true
    }
  ];

doctype html
html(lang="en")
  include ../blocks/head.pug
  body.root.page-nf

    header.header
      include ../blocks/header/root.pug
    

    main.nf
      .container

        section.breadcrumbs
          +breadcrumbs(breadcrumbs)

        section.notFound
          img(src="./img/404.png" alt="").notFound__bg
          .notFound__content
            h1.heading Упс! Путь не найден!
            .subtitle Такая страница не существует либо она была удалена.
            a(href="./").btn.btn--prim На главную 
          
        section.relatedDefault
          include ../blocks/product_list_default.pug 



    include ../blocks/footer/footer.pug